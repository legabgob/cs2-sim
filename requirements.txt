requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
torch>=2.1.0
onnx>=1.14.0
onnxscript>=0.1.0
tqdm>=4.65.0

# Optional: live HLTV data via hltvorg-api (requires Firefox + geckodriver).
# Install with: pip install hltvorg-api selenium pyvirtualdisplay
# If absent, the pipeline falls back to built-in synthetic data.
# hltvorg-api
# selenium>=4.0.0
# pyvirtualdisplay>=3.0
